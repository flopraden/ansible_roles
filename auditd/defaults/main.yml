---
# defaults file for auditd

auditd_run_services: not (ansible_virtualization_role == 'guest' and ansible_virtualization_type == 'docker')


auditd_package_names:
  - audit

auditd_service_name: auditd
